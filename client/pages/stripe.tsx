import React from "react";

export default function Stripe() {
  return (
    <div style={{ width: "100%", minHeight: "100vh" }}>
      {/* Blank page for Builder.io design — no header, navigation, or footer */}
    </div>
  );
}
